public class Average {
    public static void main (String[] args) {
        double firstNote = 7.5;
        double secondNote = 8.7;
        double media = (firstNote + secondNote) / 2;

        System.out.printf("Sua media é %.1f", media);
    }
}
